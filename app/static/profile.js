const root=document.querySelector("#mandate-content");
const money=(value,currency)=>new Intl.NumberFormat("en-IN",{style:"currency",currency,maximumFractionDigits:0}).format(value);
fetch("/buyer/mandate",{headers:{Accept:"application/json"}}).then(response=>{if(!response.ok)throw new Error("Policy unavailable");return response.json()}).then(policy=>{const auto=money(policy.automatic_purchase_limit,policy.currency),max=money(policy.maximum_transaction,policy.currency);root.setAttribute("aria-busy","false");root.replaceChildren();const values=document.createElement("div");values.className="mandate-values";[["Automatic purchase limit",auto],["Maximum transaction",max]].forEach(([label,value])=>{const item=document.createElement("div"),small=document.createElement("span"),strong=document.createElement("strong");small.textContent=label;strong.textContent=value;item.append(small,strong);values.append(item)});const scale=document.createElement("div");scale.className="mandate-scale";scale.setAttribute("aria-label",`From zero to ${auto}, the agent can proceed. Above ${auto} through ${max}, human confirmation is required. Above ${max}, transactions are blocked.`);scale.innerHTML='<div class="scale-labels"><span>₹0</span><span></span><span></span></div><div class="scale-bar"><i></i><i></i><i></i></div><div class="scale-states"><span><b>Agent can proceed</b>Auto</span><span><b>Human approval</b>Confirmation</span><span><b>Transaction blocked</b>Blocked</span></div>';scale.querySelectorAll(".scale-labels span")[1].textContent=auto;scale.querySelectorAll(".scale-labels span")[2].textContent=max;root.append(values,scale)}).catch(error=>{root.setAttribute("aria-busy","false");root.textContent=error.message});
